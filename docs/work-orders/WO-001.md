# WO-001: 공통 타입 및 날씨 상수 정의

## 메타
- **우선순위**: A01
- **상태**: 대기
- **예상 소요**: 1~2시간
- **의존성**: 없음
- **Wave**: 1

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/shared/types/weather.ts` |
| **생성** | `src/shared/types/index.ts` |
| **생성** | `src/shared/config/weather.ts` |
| **수정** | `src/shared/config/index.ts` |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **아키텍처**: FSD — shared 레이어
- **경로**: `@shared/types/`, `@shared/config/`

---

## 작업 내용

### 1. 날씨 공통 타입 정의

```typescript
// File: src/shared/types/weather.ts

/** 기상청 API 강수형태 코드 */
export type PrecipitationType = '0' | '1' | '2' | '3' | '4';

/** 기상청 API 하늘상태 코드 */
export type SkyStatus = '1' | '3' | '4';

/** 앱에서 사용하는 날씨 상태 (v1: 5종) */
export type WeatherCondition = 'sunny' | 'cloudy' | 'rainy' | 'snowy' | 'night';

/** 현재 날씨 데이터 */
export interface CurrentWeather {
  condition: WeatherCondition;
  temperature: number;
  feelsLike: number;
  humidity: number;
  description: string;
}

/** 시간별 예보 항목 */
export interface HourlyForecast {
  time: string;           // "HH:mm"
  condition: WeatherCondition;
  temperature: number;
}

/** 주간 예보 항목 */
export interface DailyForecast {
  date: string;           // "YYYY-MM-DD"
  condition: WeatherCondition;
  temperatureMin: number;
  temperatureMax: number;
}

/** 위치 정보 */
export interface LocationInfo {
  name: string;           // 도시/동네 이름
  latitude: number;
  longitude: number;
  gridX: number;          // 기상청 격자 X
  gridY: number;          // 기상청 격자 Y
}
```

### 2. 타입 index 내보내기

```typescript
// File: src/shared/types/index.ts
export type {
  PrecipitationType,
  SkyStatus,
  WeatherCondition,
  CurrentWeather,
  HourlyForecast,
  DailyForecast,
  LocationInfo,
} from './weather';
```

### 3. 날씨 → 캐릭터 상태 매핑 상수

```typescript
// File: src/shared/config/weather.ts
import type { WeatherCondition } from '@shared/types';
import { ImageSourcePropType } from 'react-native';

export interface CharacterState {
  image: ImageSourcePropType;
  expression: string;
  backgroundColor: string;
  backgroundColorDark: string;    // v2 다크모드 대비
  message: string;
}

export const WEATHER_CHARACTER_MAP: Record<WeatherCondition, CharacterState> = {
  sunny: {
    image: require('../assets/characters/monggeul-sunny.png'),
    expression: '행복한 표정',
    backgroundColor: '#87CEEB',
    backgroundColorDark: '#1E3A5F',
    message: '오늘 날씨가 정말 좋아요! 기분 좋은 하루 되세요~',
  },
  cloudy: {
    image: require('../assets/characters/monggeul-cloudy.png'),
    expression: '졸린 표정',
    backgroundColor: '#B0B0B0',
    backgroundColorDark: '#3A3A3A',
    message: '오늘은 구름이 많아요. 그래도 좋은 하루예요~',
  },
  rainy: {
    image: require('../assets/characters/monggeul-rainy.png'),
    expression: '살짝 슬픈 표정',
    backgroundColor: '#708090',
    backgroundColorDark: '#2C3E50',
    message: '비가 와요! 우산 꼭 챙기세요~',
  },
  snowy: {
    image: require('../assets/characters/monggeul-snowy.png'),
    expression: '신난 표정',
    backgroundColor: '#B0C4DE',
    backgroundColorDark: '#2C3E6B',
    message: '눈이 와요! 따뜻하게 입고 나가세요~',
  },
  night: {
    image: require('../assets/characters/monggeul-night.png'),
    expression: '나른한 표정',
    backgroundColor: '#1A1A3E',
    backgroundColorDark: '#0D0D1F',
    message: '좋은 밤이에요. 오늘도 수고했어요~',
  },
};

/** 기상청 API 코드를 WeatherCondition으로 변환 */
export function mapWeatherCode(
  pty: string,
  sky: string,
  hour: number,
): WeatherCondition {
  // 밤 시간 (18시~06시)
  if (hour >= 18 || hour < 6) return 'night';

  // 강수형태 우선 판단
  switch (pty) {
    case '1': return 'rainy';   // 비
    case '2': return 'rainy';   // 비/눈
    case '3': return 'snowy';   // 눈
    case '4': return 'rainy';   // 소나기
  }

  // 하늘상태 판단
  switch (sky) {
    case '1': return 'sunny';   // 맑음
    case '3': return 'cloudy';  // 구름많음
    case '4': return 'cloudy';  // 흐림
  }

  return 'sunny';
}
```

### 4. config index 업데이트

```typescript
// File: src/shared/config/index.ts (수정)
export * from './constants';
export {
  WEATHER_CHARACTER_MAP,
  mapWeatherCode,
} from './weather';
export type { CharacterState } from './weather';
```

---

## 완료 기준
- [ ] `src/shared/types/weather.ts` 생성 완료
- [ ] `src/shared/types/index.ts` 생성 완료
- [ ] `src/shared/config/weather.ts` 생성 완료
- [ ] `src/shared/config/index.ts` 수정 완료
- [ ] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] 절대경로 import 동작 확인

## 참고
- 이미지 파일은 WO-002에서 배치 (`src/shared/assets/characters/`)
- `mapWeatherCode`는 향후 날씨 상태 추가 시 case만 추가하면 됨
- `backgroundColorDark`는 v2 다크모드용 — v1에서는 미사용
- 참고 문서: `docs/PRD.md` 섹션 2, `docs/TECHNICAL_DESIGN.md` 섹션 2
