# WO-010: 홈 페이지 조립

## 메타
- **우선순위**: A03
- **상태**: 대기
- **예상 소요**: 2~3시간
- **의존성**: WO-006, WO-008
- **Wave**: 4

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **수정** | `src/pages/home/ui/HomePage.tsx` |
| **수정** | `src/pages/home/index.ts` |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **스타일링**: NativeWind v4
- **네비게이션**: expo-router
- **경로**: `@pages/home/`, `@widgets/`, `@features/`

---

## 작업 내용

### 1. HomePage 구현

```typescript
// File: src/pages/home/ui/HomePage.tsx
import { View, Text, TouchableOpacity, RefreshControl, ScrollView } from 'react-native';
import { useRouter } from 'expo-router';
import { useEffect, useState } from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';

import { CharacterView } from '@widgets/character-view';
import { useWeatherFetch } from '@features/weather-fetch';
import { useLocation } from '@features/location';
import { useCharacterState } from '@features/character';
import { TemperatureText } from '@entities/weather';

export function HomePage() {
  const router = useRouter();
  const { currentLocation, requestGpsLocation } = useLocation();
  const { current, isLoading, error, fetchWeather, refreshIfNeeded } = useWeatherFetch();
  const { updateForWeather, characterState } = useCharacterState();
  const [refreshing, setRefreshing] = useState(false);

  // 초기 위치 + 날씨 로딩
  useEffect(() => {
    requestGpsLocation();
  }, []);

  // 위치 변경 시 날씨 가져오기
  useEffect(() => {
    if (currentLocation) {
      fetchWeather();
    }
  }, [currentLocation]);

  // 날씨 변경 시 캐릭터 업데이트
  useEffect(() => {
    if (current) {
      updateForWeather(current.condition);
    }
  }, [current]);

  // Pull-to-refresh
  const onRefresh = async () => {
    setRefreshing(true);
    await fetchWeather();
    setRefreshing(false);
  };

  return (
    <View className="flex-1">
      <CharacterView />

      {/* 오버레이: 위치 + 날씨 정보 */}
      <SafeAreaView className="absolute inset-0" edges={['top']}>
        {/* 상단: 위치 */}
        <TouchableOpacity
          className="items-center pt-4"
          onPress={() => router.push('/city-search')}
        >
          <Text className="text-white/80 text-sm">📍 현재 위치</Text>
          <Text className="text-white text-lg font-semibold">
            {currentLocation?.name ?? '위치를 불러오는 중...'}
          </Text>
        </TouchableOpacity>

        {/* 하단: 현재 온도 + 날씨 설명 */}
        {current && (
          <View className="absolute bottom-24 left-0 right-0 items-center">
            <TemperatureText temperature={current.temperature} size="lg" />
            <Text className="text-white text-base mt-1">{current.description}</Text>
          </View>
        )}

        {/* 에러 표시 */}
        {error && (
          <View className="absolute bottom-32 left-0 right-0 items-center px-8">
            <Text className="text-red-300 text-sm text-center">{error}</Text>
          </View>
        )}
      </SafeAreaView>
    </View>
  );
}
```

### 2. 페이지 Public API (기존 유지)

```typescript
// File: src/pages/home/index.ts
export { HomePage } from './ui/HomePage';
```

---

## 완료 기준
- [ ] `HomePage` — 몽글이 + 현재 날씨 + 위치 표시
- [ ] 위치명 탭 → 도시 검색 모달 열림
- [ ] Pull-to-refresh 동작
- [ ] 에러 시 에러 메시지 표시
- [ ] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] [디바이스] 앱 실행 → GPS 위치 감지 → 날씨 로딩 → 몽글이 표시 확인
- [ ] [디바이스] 캐릭터 터치 상호작용 확인
- [ ] [디바이스] 위치명 탭 → 도시 검색 모달 확인

## 참고
- CharacterView(WO-008)가 전체 배경을 채우고, 날씨 정보는 오버레이로 표시
- features 간 데이터 흐름: location → weather-fetch → character
- 캐릭터가 주인공 — 날씨 정보는 보조적으로 표시 (PRD 설계 원칙)
- 참고 문서: `docs/PRD.md` 섹션 4, `docs/TECHNICAL_DESIGN.md` 섹션 1, 6
