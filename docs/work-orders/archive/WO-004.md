# WO-004: Weather 엔티티 (도메인 모델 + UI 컴포넌트)

## 메타
- **우선순위**: A02
- **상태**: 완료
- **예상 소요**: 1~2시간
- **의존성**: WO-001
- **Wave**: 2

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/entities/weather/model/types.ts` |
| **생성** | `src/entities/weather/ui/WeatherIcon.tsx` |
| **생성** | `src/entities/weather/ui/TemperatureText.tsx` |
| **생성** | `src/entities/weather/index.ts` |
| **삭제** | `src/entities/example/` (템플릿 예제 제거) |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **스타일링**: NativeWind v4
- **아키텍처**: FSD — entities 레이어
- **경로**: `@entities/weather/`, `@shared/types/`

---

## 작업 내용

### 1. Weather 엔티티 타입

```typescript
// File: src/entities/weather/model/types.ts
import type { WeatherCondition } from '@shared/types';

export interface WeatherIconProps {
  condition: WeatherCondition;
  size?: number;
}

export interface TemperatureTextProps {
  temperature: number;
  unit?: 'C' | 'F';
  size?: 'sm' | 'md' | 'lg';
}
```

### 2. 날씨 아이콘 컴포넌트

```typescript
// File: src/entities/weather/ui/WeatherIcon.tsx
import { View, Text } from 'react-native';
import type { WeatherIconProps } from '../model/types';
import type { WeatherCondition } from '@shared/types';

const WEATHER_EMOJI: Record<WeatherCondition, string> = {
  sunny: '☀️',
  cloudy: '☁️',
  rainy: '🌧️',
  snowy: '❄️',
  night: '🌙',
};

export function WeatherIcon({ condition, size = 24 }: WeatherIconProps) {
  return (
    <View className="items-center justify-center">
      <Text style={{ fontSize: size }}>{WEATHER_EMOJI[condition]}</Text>
    </View>
  );
}
```

### 3. 온도 텍스트 컴포넌트

```typescript
// File: src/entities/weather/ui/TemperatureText.tsx
import { Text } from 'react-native';
import type { TemperatureTextProps } from '../model/types';

const SIZE_CLASSES = {
  sm: 'text-base text-gray-600',
  md: 'text-2xl font-semibold text-gray-800',
  lg: 'text-5xl font-bold text-gray-900',
} as const;

export function TemperatureText({
  temperature,
  unit = 'C',
  size = 'md',
}: TemperatureTextProps) {
  const symbol = unit === 'C' ? '°' : '°F';

  return (
    <Text className={SIZE_CLASSES[size]}>
      {Math.round(temperature)}{symbol}
    </Text>
  );
}
```

### 4. 엔티티 Public API

```typescript
// File: src/entities/weather/index.ts
export { WeatherIcon } from './ui/WeatherIcon';
export { TemperatureText } from './ui/TemperatureText';
export type { WeatherIconProps, TemperatureTextProps } from './model/types';
```

### 5. 템플릿 예제 제거

`src/entities/example/` 폴더 전체 삭제 (템플릿 예제이므로 불필요)

---

## 완료 기준
- [x] `src/entities/weather/` 슬라이스 생성 완료
- [x] `WeatherIcon` — 5가지 날씨 상태 이모지 표시
- [x] `TemperatureText` — sm/md/lg 사이즈 지원
- [x] `src/entities/example/` 삭제 완료
- [x] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [x] 절대경로 import 동작 확인 (`@entities/weather`)

## 참고
- WeatherIcon은 v2에서 커스텀 아이콘(SVG/Skia)으로 교체 가능
- 현재는 이모지로 간단하게 구현 — 시간별/주간 리스트에서 사용
- entities 레이어이므로 shared만 import 가능
- 참고 문서: `docs/TECHNICAL_DESIGN.md` 섹션 1
