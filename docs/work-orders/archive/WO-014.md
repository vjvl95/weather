# WO-014: Character View — Phase 2 분위기 강화 (이펙트 + 그림자)

## 메타
- **우선순위**: A02
- **상태**: 완료
- **예상 소요**: 3~4시간
- **의존성**: WO-008
- **Wave**: 3.5

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/widgets/character-view/ui/AmbientEffects.tsx` |
| **생성** | `src/widgets/character-view/ui/CharacterShadow.tsx` |
| **수정** | `src/widgets/character-view/ui/CharacterView.tsx` (레이어 추가) |
| **수정** | `src/widgets/character-view/ui/CharacterSprite.tsx` (reactHappy/reactComfort + mood prop 추가) |
| **생성** | `src/shared/assets/effects/` (이펙트 에셋) |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **애니메이션**: react-native-reanimated 4.x
- **그래픽**: @shopify/react-native-skia (파티클 효과 시)
- **경로**: `@widgets/character-view/`, `@shared/`

---

## V1 아키텍처 기준 (섹션 5-2, 12 Phase 2)

- 날씨별 배경색 전환 강화 (시간대 톤 보정)
- 비/눈/별/안개 앰비언트 이펙트 레이어
- 캐릭터 아래 그림자 반응 (translateY 반비례)

---

## 작업 내용

### 1. CharacterShadow — 캐릭터 그림자

```typescript
// File: src/widgets/character-view/ui/CharacterShadow.tsx
import Animated, {
  useAnimatedStyle,
  interpolate,
  SharedValue,
} from 'react-native-reanimated';

interface CharacterShadowProps {
  /**
   * idle 부유 모션의 translateY만 전달 (anchorY 이동값 제외)
   *
   * ⚠️ 중요: 이 값은 CharacterSprite의 idleOffsetY만 반영해야 함
   * anchorY 기반 위치 이동은 포함하면 안 됨
   * → 그렇지 않으면 화면 이동할 때마다 그림자가 늘었다 줄었다 하는 어색한 깊이감 발생
   *
   * Phase 3 (WO-015)에서 anchorY 이동이 추가될 때:
   * - CharacterSprite 내부에서 idleOffsetY와 anchorOffsetY를 분리
   * - 그림자에는 idleOffsetY만 전달
   */
  idleOffsetY: SharedValue<number>;
}

/**
 * 캐릭터 아래 타원형 그림자
 *
 * 캐릭터가 idle 모션으로 위로 올라갈수록:
 * - 그림자 scaleX 커짐 (넓어짐)
 * - 그림자 opacity 줄어듦 (흐려짐)
 *
 * 작은 요소지만 생동감을 크게 올린다 (V1 아키텍처 섹션 5-2)
 */
export function CharacterShadow({ idleOffsetY }: CharacterShadowProps) {
  const shadowStyle = useAnimatedStyle(() => {
    const scaleX = interpolate(idleOffsetY.value, [-10, 0], [1.15, 1]);
    const opacity = interpolate(idleOffsetY.value, [-10, 0], [0.15, 0.3]);

    return {
      transform: [{ scaleX }, { scaleY: 0.3 }],
      opacity,
    };
  });

  return (
    <Animated.View
      style={[
        {
          width: 120,
          height: 40,
          borderRadius: 60,
          backgroundColor: '#000',
          alignSelf: 'center',
          marginTop: -10,
        },
        shadowStyle,
      ]}
    />
  );
}
```

### 2. AmbientEffects — 날씨 이펙트 레이어

```typescript
// File: src/widgets/character-view/ui/AmbientEffects.tsx
import { View, StyleSheet, Dimensions } from 'react-native';
import Animated, {
  useSharedValue,
  useAnimatedStyle,
  withRepeat,
  withTiming,
  withDelay,
  useReducedMotion,
  Easing,
} from 'react-native-reanimated';
import { useEffect, useMemo } from 'react';
import type { EffectPreset } from '@shared/types';

const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get('window');

interface AmbientEffectsProps {
  effectPreset: EffectPreset;
}

/** 파티클 하나의 설정 */
interface Particle {
  id: number;
  startX: number;
  delay: number;
  duration: number;
  size: number;
  opacity: number;
}

/**
 * 날씨별 앰비언트 이펙트 레이어
 *
 * - rain-fall: 비 내리기 (세로 직선)
 * - snow-fall: 눈 내리기 (느리게 + 좌우 흔들림)
 * - night-stars: 별 반짝임 (opacity 깜빡)
 * - sun-glow: 부드러운 광원 (Phase 2 기본: 투명 오버레이)
 * - cloud-drift: 안개/구름 흐름 (Phase 2 기본: 투명 오버레이)
 *
 * 주의:
 * - 캐릭터보다 뒤에 위치 (zIndex 낮게)
 * - 과도한 밀도 금지 (V1 아키텍처 섹션 5-2)
 * - Reduced Motion 시 밀도 감소
 */
export function AmbientEffects({ effectPreset }: AmbientEffectsProps) {
  const reduceMotion = useReducedMotion();

  switch (effectPreset) {
    case 'rain-fall':
      return <RainEffect count={reduceMotion ? 5 : 20} />;
    case 'snow-fall':
      return <SnowEffect count={reduceMotion ? 5 : 15} />;
    case 'night-stars':
      return <StarEffect count={reduceMotion ? 3 : 10} />;
    case 'sun-glow':
    case 'cloud-drift':
      // Phase 2 기본: 미묘한 오버레이만
      return null;
    default:
      return null;
  }
}

// --- 비 효과 ---

function RainDrop({ startX, delay, duration }: { startX: number; delay: number; duration: number }) {
  const reduceMotion = useReducedMotion();
  const translateY = useSharedValue(-20);

  useEffect(() => {
    if (reduceMotion) {
      // ⚠️ withRepeat(-1) 금지 (CLAUDE.md ANR 규칙)
      // 정적 고정도 금지 (화면 상단에 선이 멈춰 보임)
      // → JS 타이머로 느린 1회 시퀀스를 반복 트리거
      const slowDuration = duration * 2;
      const runOnce = () => {
        translateY.value = -20;
        translateY.value = withDelay(
          delay,
          withTiming(SCREEN_HEIGHT + 20, { duration: slowDuration, easing: Easing.linear }),
        );
      };
      runOnce();
      const interval = setInterval(runOnce, slowDuration + delay);
      return () => clearInterval(interval);
    }

    translateY.value = withDelay(
      delay,
      withRepeat(
        withTiming(SCREEN_HEIGHT + 20, { duration, easing: Easing.linear }),
        -1,
      ),
    );
  }, [reduceMotion, delay, duration, translateY]);

  const style = useAnimatedStyle(() => ({
    transform: [{ translateY: translateY.value }],
  }));

  return (
    <Animated.View
      style={[
        {
          position: 'absolute',
          left: startX,
          width: 1.5,
          height: 18,
          backgroundColor: 'rgba(174,194,224,0.5)',
          borderRadius: 1,
        },
        style,
      ]}
    />
  );
}

function RainEffect({ count }: { count: number }) {
  const particles = useMemo(() =>
    generateParticles(count, { durationMin: 800, durationMax: 1200 }),
    [count],
  );

  return (
    <View style={StyleSheet.absoluteFill} pointerEvents="none">
      {particles.map((p) => (
        <RainDrop key={p.id} startX={p.startX} delay={p.delay} duration={p.duration} />
      ))}
    </View>
  );
}

// --- 눈 효과 ---

function Snowflake({ startX, delay, duration, size }: {
  startX: number; delay: number; duration: number; size: number;
}) {
  const reduceMotion = useReducedMotion();
  const translateY = useSharedValue(-20);
  const translateX = useSharedValue(0);

  useEffect(() => {
    if (reduceMotion) {
      // ⚠️ withRepeat(-1) 금지 → JS 타이머로 느린 1회 시퀀스 반복
      const slowDuration = duration * 2;
      const runOnce = () => {
        translateY.value = -20;
        translateX.value = 0;
        translateY.value = withDelay(
          delay,
          withTiming(SCREEN_HEIGHT + 20, { duration: slowDuration, easing: Easing.linear }),
        );
      };
      runOnce();
      const interval = setInterval(runOnce, slowDuration + delay);
      return () => clearInterval(interval);
    }

    translateY.value = withDelay(
      delay,
      withRepeat(
        withTiming(SCREEN_HEIGHT + 20, { duration, easing: Easing.linear }),
        -1,
      ),
    );

    // 좌우 미세 흔들림
    translateX.value = withDelay(
      delay,
      withRepeat(
        withTiming(20, { duration: duration / 2, easing: Easing.inOut(Easing.ease) }),
        -1,
        true,
      ),
    );
  }, [reduceMotion, delay, duration, translateY, translateX]);

  const style = useAnimatedStyle(() => ({
    transform: [
      { translateY: translateY.value },
      { translateX: translateX.value },
    ],
  }));

  return (
    <Animated.View
      style={[
        {
          position: 'absolute',
          left: startX,
          width: size,
          height: size,
          backgroundColor: 'rgba(255,255,255,0.7)',
          borderRadius: size / 2,
        },
        style,
      ]}
    />
  );
}

function SnowEffect({ count }: { count: number }) {
  const particles = useMemo(() =>
    generateParticles(count, { durationMin: 3000, durationMax: 5000, sizeMin: 4, sizeMax: 8 }),
    [count],
  );

  return (
    <View style={StyleSheet.absoluteFill} pointerEvents="none">
      {particles.map((p) => (
        <Snowflake key={p.id} startX={p.startX} delay={p.delay} duration={p.duration} size={p.size} />
      ))}
    </View>
  );
}

// --- 별 효과 ---

function Star({ startX, startY, delay, size }: {
  startX: number; startY: number; delay: number; size: number;
}) {
  const reduceMotion = useReducedMotion();
  const opacity = useSharedValue(0.3);

  useEffect(() => {
    if (reduceMotion) {
      // ⚠️ withRepeat(-1) 금지 → JS 타이머로 느린 깜빡임
      const slowDuration = 4000;
      const runOnce = () => {
        opacity.value = withTiming(1, { duration: slowDuration, easing: Easing.inOut(Easing.ease) });
        setTimeout(() => {
          opacity.value = withTiming(0.3, { duration: slowDuration, easing: Easing.inOut(Easing.ease) });
        }, slowDuration);
      };
      runOnce();
      const interval = setInterval(runOnce, slowDuration * 2);
      return () => clearInterval(interval);
    }

    opacity.value = withDelay(
      delay,
      withRepeat(
        withTiming(1, { duration: 2000, easing: Easing.inOut(Easing.ease) }),
        -1,
        true,
      ),
    );
  }, [reduceMotion, delay, opacity]);

  const style = useAnimatedStyle(() => ({
    opacity: opacity.value,
  }));

  return (
    <Animated.View
      style={[
        {
          position: 'absolute',
          left: startX,
          top: startY,
          width: size,
          height: size,
          backgroundColor: 'rgba(255,255,240,0.9)',
          borderRadius: size / 2,
        },
        style,
      ]}
    />
  );
}

function StarEffect({ count }: { count: number }) {
  const stars = useMemo(() => {
    const rng = seedRng(42);
    return Array.from({ length: count }, (_, i) => ({
      id: i,
      startX: rng() * SCREEN_WIDTH,
      startY: rng() * SCREEN_HEIGHT * 0.5, // 상단 절반에만
      delay: rng() * 3000,
      size: 2 + rng() * 3,
    }));
  }, [count]);

  return (
    <View style={StyleSheet.absoluteFill} pointerEvents="none">
      {stars.map((s) => (
        <Star key={s.id} startX={s.startX} startY={s.startY} delay={s.delay} size={s.size} />
      ))}
    </View>
  );
}

// --- 유틸 ---

/** 시드 기반 RNG (CLAUDE.md: Math.random 금지) */
function seedRng(seed: number): () => number {
  return () => {
    let t = (seed += 0x6d2b79f5);
    t = Math.imul(t ^ (t >>> 15), t | 1);
    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function generateParticles(
  count: number,
  opts: { durationMin: number; durationMax: number; sizeMin?: number; sizeMax?: number },
): Particle[] {
  const rng = seedRng(123);
  return Array.from({ length: count }, (_, i) => ({
    id: i,
    startX: rng() * SCREEN_WIDTH,
    delay: rng() * 2000,
    duration: opts.durationMin + rng() * (opts.durationMax - opts.durationMin),
    size: (opts.sizeMin ?? 4) + rng() * ((opts.sizeMax ?? 8) - (opts.sizeMin ?? 4)),
    opacity: 0.3 + rng() * 0.5,
  }));
}
```

### 3. CharacterView 레이어 추가

```typescript
// File: src/widgets/character-view/ui/CharacterView.tsx (수정)
//
// Phase 1 구조에 AmbientEffects, CharacterShadow 삽입
//
// 변경 사항:
// - AmbientEffects 레이어 추가 (캐릭터 뒤)
// - CharacterShadow 추가 (캐릭터 아래)
// - CharacterSprite에서 translateY SharedValue를 외부로 노출 필요
//   → CharacterSprite에 ref 또는 translateY prop 추가

import { View } from 'react-native';
import { useSharedValue } from 'react-native-reanimated';
import { useCharacterState } from '@features/character';
import { WeatherBackground } from './WeatherBackground';
import { AmbientEffects } from './AmbientEffects';
import { CharacterShadow } from './CharacterShadow';
import { CharacterSprite } from './CharacterSprite';
import { SpeechBubble } from './SpeechBubble';

export function CharacterView() {
  const {
    presentation,
    runtime,
    onCharacterTap,
    onPetStart,
    onPetEnd,
    hideBubble,
  } = useCharacterState();

  // 그림자 연동: idle 부유 모션의 Y 오프셋만 (anchor 이동값 제외)
  // ⚠️ anchorOffsetY와 분리 필수 — 그림자는 둥실거림만 반영
  const idleOffsetY = useSharedValue(0);

  if (!presentation) return null;

  return (
    <WeatherBackground colors={presentation.backgroundColors}>
      {/* Layer 2: 앰비언트 이펙트 (캐릭터 뒤) */}
      <AmbientEffects effectPreset={presentation.effectPreset} />

      <View className="flex-1 items-center justify-center">
        {/* Layer 5: 말풍선 */}
        <SpeechBubble
          message={runtime.currentBubbleMessage}
          visible={runtime.isBubbleVisible}
          onAutoHide={hideBubble}
        />

        {/* Layer 4: 캐릭터 본체 */}
        <CharacterSprite
          image={presentation.asset.image}
          motionPreset={presentation.motionPreset}
          onTap={onCharacterTap}
          onPetStart={onPetStart}
          onPetEnd={onPetEnd}
          idleOffsetYOutput={idleOffsetY}
        />

        {/* Layer 3: 그림자 (캐릭터 바로 아래) — idle 모션만 반영 */}
        <CharacterShadow idleOffsetY={idleOffsetY} />
      </View>
    </WeatherBackground>
  );
}
```

> **참고**: CharacterSprite에 `idleOffsetYOutput` prop을 추가해야 합니다.
> Phase 1의 내부 idle translateY를 외부 SharedValue로 동기화하는 코드 1줄 추가.
>
> **⚠️ 분리 원칙**: CharacterSprite 내부에서 `idleOffsetY`(둥실거림)와
> `anchorOffsetY`(위치 이동, Phase 3에서 추가)를 별도 SharedValue로 관리.
> 최종 transform에서는 둘을 합산하되, 그림자에는 `idleOffsetY`만 전달.
> 이렇게 하지 않으면 캐릭터 위치 이동 시 그림자가 어색하게 늘었다 줄었다 함.

---

### 4. 상호작용 리액션 모션

V1 아키텍처 섹션 8-2의 `reactHappy`, `reactComfort`를 CharacterSprite에 추가합니다.

```typescript
// CharacterSprite.tsx에 추가할 리액션 모션
//
// reactHappy (맑음/눈 날씨 탭 시):
//   scale 1 → 1.2 → 1 + 약간 위로 점프 (translateY -15 → 0)
//   트리거: 탭 시 mood가 'happy' | 'excited'이면 tapBounce 대신 사용
//
// reactComfort (비/밤 날씨 탭 시):
//   살짝 웅크림 (scaleY 1 → 0.92 → 1) + 느린 스프링
//   트리거: 탭 시 mood가 'cozy' | 'calm' | 'sad'이면 tapBounce 대신 사용
//
// 구현: CharacterSpriteProps에 mood 추가
//   onTap 핸들러에서 mood에 따라 분기
//
// interface CharacterSpriteProps {
//   ...
//   mood: CharacterMood;  // ← 추가
// }
//
// tapGesture.onEnd(() => {
//   if (mood === 'happy' || mood === 'excited') {
//     // reactHappy
//     scale.value = withSequence(
//       withSpring(1.2, { damping: 4, stiffness: 300 }),
//       withSpring(1, { damping: 8 }),
//     );
//     translateY.value = withSequence(
//       withTiming(-15, { duration: 200 }),
//       withSpring(0),
//     );
//   } else {
//     // reactComfort
//     scale.value = withSequence(
//       withTiming(0.92, { duration: 300 }),
//       withSpring(1, { damping: 12 }),
//     );
//   }
//   onTap();
// });
```

---

## 완료 기준
- [x] `AmbientEffects.tsx` 생성 완료
- [x] `CharacterShadow.tsx` 생성 완료
- [x] `CharacterView.tsx` 레이어 구조 업데이트
- [x] 비 이펙트 (`rain-fall`) — 세로 직선 파티클 애니메이션
- [x] 눈 이펙트 (`snow-fall`) — 느린 낙하 + 좌우 흔들림
- [x] 별 이펙트 (`night-stars`) — 불규칙 깜빡임
- [x] 그림자 — 캐릭터 idleOffsetY 반비례 크기/투명도 변화
- [x] reactHappy — 맑음/눈 탭 시 밝은 리액션
- [x] reactComfort — 비/밤 탭 시 위로 리액션
- [x] Reduced Motion 시 파티클: JS 타이머 느린 시퀀스 적용
- [x] 시드 기반 RNG 사용 (mulberry32 from @shared/lib)
- [x] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] [디바이스] 비/눈/별 이펙트 자연스러운 동작 확인
- [ ] [디바이스] Reduced Motion ON에서 이펙트가 정적 고정 없이 느리게 움직이는지 확인
- [ ] [디바이스] 그림자 캐릭터와 연동 확인
- [ ] [디바이스] 이펙트가 캐릭터보다 뒤에 위치하는지 확인
- [ ] [디바이스] reactHappy/reactComfort가 mood에 따라 분기 동작 확인

## 참고
- V1 아키텍처: `docs/CHARACTER_V1_ARCHITECTURE.md` 섹션 5-2, 8-2, 12 (Phase 2)
- sun-glow, cloud-drift는 Phase 2 기본 구현에서 null 반환 → 향후 Skia 셰이더로 확장 가능
- 파티클 수: 비(20), 눈(15), 별(10) — Reduced Motion 시 각각 3, 3, 3 (JS 타이머 반복)
- 이펙트는 캐릭터 앞에 오지 않는다 (V1 아키텍처 원칙)
- reactHappy/reactComfort는 기존 tapBounce를 mood 기반으로 분기한 것 — 추가 파일 없음
