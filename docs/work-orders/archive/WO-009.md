# WO-009: Forecast 위젯 (시간별 + 주간 예보)

## 메타
- **우선순위**: A02
- **상태**: 완료
- **예상 소요**: 2~3시간
- **의존성**: WO-004
- **Wave**: 3

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/widgets/hourly-forecast/ui/HourlyForecast.tsx` |
| **생성** | `src/widgets/hourly-forecast/ui/HourlyItem.tsx` |
| **생성** | `src/widgets/hourly-forecast/index.ts` |
| **생성** | `src/widgets/weekly-forecast/ui/WeeklyForecast.tsx` |
| **생성** | `src/widgets/weekly-forecast/ui/WeeklyItem.tsx` |
| **생성** | `src/widgets/weekly-forecast/index.ts` |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **스타일링**: NativeWind v4
- **리스트**: @legendapp/list (또는 FlatList)
- **경로**: `@widgets/`, `@entities/weather/`, `@shared/`

---

## 작업 내용

### 1. 시간별 예보 개별 항목

```typescript
// File: src/widgets/hourly-forecast/ui/HourlyItem.tsx
import { View, Text } from 'react-native';
import { WeatherIcon, TemperatureText } from '@entities/weather';
import type { HourlyForecast } from '@shared/types';

interface HourlyItemProps {
  item: HourlyForecast;
  isNow?: boolean;
}

export function HourlyItem({ item, isNow = false }: HourlyItemProps) {
  return (
    <View className={`items-center px-4 py-3 ${isNow ? 'bg-white/20 rounded-2xl' : ''}`}>
      <Text className={`text-sm mb-2 ${isNow ? 'font-bold text-white' : 'text-white/80'}`}>
        {isNow ? '지금' : item.time}
      </Text>
      <WeatherIcon condition={item.condition} size={28} />
      <TemperatureText temperature={item.temperature} size="sm" />
    </View>
  );
}
```

### 2. 시간별 예보 리스트

```typescript
// File: src/widgets/hourly-forecast/ui/HourlyForecast.tsx
import { View, Text, ScrollView } from 'react-native';
import type { HourlyForecast as HourlyForecastType } from '@shared/types';
import { HourlyItem } from './HourlyItem';

interface HourlyForecastProps {
  forecasts: HourlyForecastType[];
}

export function HourlyForecast({ forecasts }: HourlyForecastProps) {
  if (forecasts.length === 0) {
    return (
      <View className="items-center py-8">
        <Text className="text-gray-400">시간별 예보를 불러오는 중이에요...</Text>
      </View>
    );
  }

  return (
    <View className="flex-1">
      <ScrollView
        horizontal
        showsHorizontalScrollIndicator={false}
        contentContainerStyle={{ paddingHorizontal: 16 }}
      >
        {forecasts.map((item, index) => (
          <HourlyItem
            key={`${item.time}-${index}`}
            item={item}
            isNow={index === 0}
          />
        ))}
      </ScrollView>
    </View>
  );
}
```

### 3. 시간별 위젯 Public API

```typescript
// File: src/widgets/hourly-forecast/index.ts
export { HourlyForecast } from './ui/HourlyForecast';
```

### 4. 주간 예보 개별 항목

```typescript
// File: src/widgets/weekly-forecast/ui/WeeklyItem.tsx
import { View, Text } from 'react-native';
import { WeatherIcon, TemperatureText } from '@entities/weather';
import type { DailyForecast } from '@shared/types';

interface WeeklyItemProps {
  item: DailyForecast;
  isToday?: boolean;
}

const DAY_NAMES = ['일', '월', '화', '수', '목', '금', '토'];

export function WeeklyItem({ item, isToday = false }: WeeklyItemProps) {
  const date = new Date(item.date);
  const dayName = DAY_NAMES[date.getDay()];

  return (
    <View className={`flex-row items-center justify-between px-6 py-4 ${isToday ? 'bg-white/10 rounded-2xl' : ''}`}>
      {/* 요일 */}
      <Text className={`w-12 text-base ${isToday ? 'font-bold text-white' : 'text-white/80'}`}>
        {isToday ? '오늘' : dayName}
      </Text>

      {/* 날씨 아이콘 */}
      <WeatherIcon condition={item.condition} size={28} />

      {/* 최저/최고 온도 */}
      <View className="flex-row items-center gap-3">
        <TemperatureText temperature={item.temperatureMin} size="sm" />
        <Text className="text-white/40">/</Text>
        <TemperatureText temperature={item.temperatureMax} size="sm" />
      </View>
    </View>
  );
}
```

### 5. 주간 예보 리스트

```typescript
// File: src/widgets/weekly-forecast/ui/WeeklyForecast.tsx
import { View, Text } from 'react-native';
import type { DailyForecast } from '@shared/types';
import { WeeklyItem } from './WeeklyItem';

interface WeeklyForecastProps {
  forecasts: DailyForecast[];
}

export function WeeklyForecast({ forecasts }: WeeklyForecastProps) {
  if (forecasts.length === 0) {
    return (
      <View className="items-center py-8">
        <Text className="text-gray-400">주간 예보를 불러오는 중이에요...</Text>
      </View>
    );
  }

  return (
    <View className="flex-1 px-4">
      {forecasts.map((item, index) => (
        <WeeklyItem
          key={item.date}
          item={item}
          isToday={index === 0}
        />
      ))}
    </View>
  );
}
```

### 6. 주간 위젯 Public API

```typescript
// File: src/widgets/weekly-forecast/index.ts
export { WeeklyForecast } from './ui/WeeklyForecast';
```

---

## 완료 기준
- [x] `src/widgets/hourly-forecast/` 위젯 생성 완료
- [x] `src/widgets/weekly-forecast/` 위젯 생성 완료
- [x] `HourlyForecast` — 가로 스크롤 시간별 리스트
- [x] `WeeklyForecast` — 세로 리스트 주간 예보
- [x] 빈 데이터 시 로딩 메시지 표시
- [x] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] [디바이스] 시간별 예보 가로 스크롤 동작
- [ ] [디바이스] 주간 예보 리스트 표시 확인

## 참고
- entities/weather의 `WeatherIcon`, `TemperatureText` 사용 (WO-004)
- 데이터는 pages에서 props로 전달 (WO-010, 011에서 연결)
- v2에서 리스트 최적화 필요 시 `@legendapp/list`로 교체
- 참고 문서: `docs/TECHNICAL_DESIGN.md` 섹션 1
