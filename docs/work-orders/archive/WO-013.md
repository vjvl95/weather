# WO-013: 통합 테스트 및 테마 연동

## 메타
- **우선순위**: A03
- **상태**: 완료
- **예상 소요**: 2~3시간
- **의존성**: WO-010, WO-011, WO-012, WO-016
- **Wave**: 5

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **수정** | `src/features/theme-manager/model/useThemeStore.ts` |
| **수정** | `src/features/theme-manager/lib/useTheme.ts` |
| **수정** | `src/features/theme-manager/model/types.ts` |
| **수정** | `app/_layout.tsx` |
| **수정** | `app/(tabs)/_layout.tsx` |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **상태관리**: Zustand 5.x
- **네비게이션**: expo-router
- **경로**: `@features/theme-manager/`, `app/`

---

## 작업 내용

### 1. 테마를 날씨 배경색과 연동

```typescript
// File: src/features/theme-manager/model/types.ts (수정)
// 기존 타입 유지 + 추가

export interface ThemeState {
  mode: ThemeMode;
  colors: ColorTokens;
  typography: TypographyTokens;
  spacing: SpacingTokens;
  /** 현재 날씨 배경색 */
  weatherBackgroundColor: string;
}
```

```typescript
// File: src/features/theme-manager/model/useThemeStore.ts (수정)
// 기존 스토어에 추가

setWeatherBackgroundColor: (color: string) =>
  set({ weatherBackgroundColor: color }),
```

```typescript
// File: src/features/theme-manager/lib/useTheme.ts (수정)
// useTheme 훅에 weatherBackgroundColor 노출

export function useTheme() {
  // ... 기존 코드 유지
  const { weatherBackgroundColor, setWeatherBackgroundColor } = useThemeStore();

  return {
    // ... 기존 반환값 유지
    weatherBackgroundColor,
    setWeatherBackgroundColor,
  };
}
```

### 2. RootLayout 업데이트 — 앱 초기화 흐름

```typescript
// File: app/_layout.tsx (수정)
// 추가할 내용:
// 1. hydrateLocationStore 호출
// 2. detail 라우트 제거
// 3. city-search 모달 추가

import { hydrateLocationStore } from '@features/location';

// prepare 함수 내:
useEffect(() => {
  async function prepare() {
    await hydrateLocationStore();
    setAppIsReady(true);
  }
  prepare();
}, []);

// Stack 내:
// <Stack.Screen name="detail" ... /> 제거
// <Stack.Screen name="city-search" options={{ presentation: 'modal', headerShown: false }} /> 추가
```

### 3. TabLayout 업데이트 — 날씨 배경색 연동

```typescript
// File: app/(tabs)/_layout.tsx (수정)
// 탭바 배경색을 날씨에 맞게 조정 (선택)
// 기본은 WO-002에서 설정한 흰색 유지
// 필요 시 여기서 투명 탭바로 변경 가능
```

### 4. 전체 데이터 흐름 검증

앱 실행 시 흐름:
```
1. RootLayout: hydrateLocationStore() → 저장된 위치 복원
2. SplashScreen 숨기기
3. HomePage: requestGpsLocation() → GPS 위치 감지
4. HomePage: fetchWeather() → 기상청 API 호출
5. HomePage: updateForWeather() → 캐릭터 상태 변경
6. CharacterView: 몽글이 표시 + 애니메이션 시작
```

---

## 완료 기준
- [x] 테마 스토어에 날씨 배경색 연동 추가 (`weatherBackgroundColor`, `setWeatherBackgroundColor`)
- [x] RootLayout — hydrateLocationStore 호출
- [x] city-search 모달 라우트 (WO-002에서 이미 추가)
- [x] detail 라우트 제거 (WO-002에서 이미 삭제)
- [ ] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`) — node_modules 미설치, 코드 수준 검증 완료
- [ ] [디바이스] 앱 실행 → GPS → 날씨 로딩 → 몽글이 표시 전체 흐름 확인
- [ ] [디바이스] 탭 전환 정상 동작 (홈/시간별/주간/설정)
- [ ] [디바이스] 도시 검색 → 도시 변경 → 날씨 갱신 → 몽글이 변경 확인
- [ ] [디바이스] 앱 종료 후 재실행 → 마지막 위치 복원 확인

## 참고
- 이 WO는 모든 기능이 올바르게 연결되는지 검증하는 통합 WO
- 개별 기능의 버그는 해당 WO로 돌아가서 수정
- 참고 문서: `docs/TECHNICAL_DESIGN.md` 섹션 4 (Store 간 데이터 흐름)
