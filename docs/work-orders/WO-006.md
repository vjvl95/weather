# WO-006: Weather Fetch Feature (날씨 데이터 상태관리)

## 메타
- **우선순위**: A02
- **상태**: 대기
- **예상 소요**: 2~3시간
- **의존성**: WO-003, WO-005
- **Wave**: 3

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/features/weather-fetch/model/types.ts` |
| **생성** | `src/features/weather-fetch/model/useWeatherStore.ts` |
| **생성** | `src/features/weather-fetch/lib/useWeatherFetch.ts` |
| **생성** | `src/features/weather-fetch/index.ts` |

---

## 기술 스택 요약
- **언어**: TypeScript (strict mode)
- **상태관리**: Zustand 5.x
- **API**: 기상청 단기예보/중기예보 (WO-003에서 구현된 유틸 사용)
- **경로**: `@features/weather-fetch/`, `@shared/`, `@features/location/`

---

## 작업 내용

### 1. Weather Store 타입

```typescript
// File: src/features/weather-fetch/model/types.ts
import type { CurrentWeather, HourlyForecast, DailyForecast } from '@shared/types';

export interface WeatherState {
  /** 현재 날씨 */
  current: CurrentWeather | null;
  /** 시간별 예보 (최대 24개) */
  hourly: HourlyForecast[];
  /** 주간 예보 (최대 7개) */
  daily: DailyForecast[];
  /** 로딩 상태 */
  isLoading: boolean;
  /** 에러 메시지 */
  error: string | null;
  /** 마지막 갱신 시간 */
  lastFetchedAt: number | null;
}

export interface WeatherActions {
  setCurrent: (weather: CurrentWeather) => void;
  setHourly: (forecasts: HourlyForecast[]) => void;
  setDaily: (forecasts: DailyForecast[]) => void;
  setLoading: (loading: boolean) => void;
  setError: (error: string | null) => void;
  setLastFetchedAt: (time: number) => void;
  reset: () => void;
}
```

### 2. Weather Zustand Store

```typescript
// File: src/features/weather-fetch/model/useWeatherStore.ts
import { create } from 'zustand';
import type { WeatherState, WeatherActions } from './types';

type WeatherStore = WeatherState & WeatherActions;

const initialState: WeatherState = {
  current: null,
  hourly: [],
  daily: [],
  isLoading: false,
  error: null,
  lastFetchedAt: null,
};

export const useWeatherStore = create<WeatherStore>((set) => ({
  ...initialState,

  setCurrent: (weather) => set({ current: weather }),
  setHourly: (forecasts) => set({ hourly: forecasts }),
  setDaily: (forecasts) => set({ daily: forecasts }),
  setLoading: (loading) => set({ isLoading: loading }),
  setError: (error) => set({ error }),
  setLastFetchedAt: (time) => set({ lastFetchedAt: time }),
  reset: () => set(initialState),
}));
```

### 3. 날씨 데이터 가져오기 훅

```typescript
// File: src/features/weather-fetch/lib/useWeatherFetch.ts
import { useCallback } from 'react';
import { useWeatherStore } from '../model/useWeatherStore';
import { useLocationStore } from '@features/location';
import {
  fetchShortForecast,
  parseCurrentWeather,
  parseHourlyForecast,
} from '@shared/lib';
import { API_CONFIG } from '@shared/config';

export function useWeatherFetch() {
  const {
    current,
    hourly,
    daily,
    isLoading,
    error,
    lastFetchedAt,
    setCurrent,
    setHourly,
    setDaily,
    setLoading,
    setError,
    setLastFetchedAt,
  } = useWeatherStore();

  const { currentLocation } = useLocationStore();

  /** 날씨 데이터 갱신이 필요한지 확인 */
  const needsRefresh = useCallback((): boolean => {
    if (!lastFetchedAt) return true;
    return Date.now() - lastFetchedAt > API_CONFIG.REFRESH_INTERVAL;
  }, [lastFetchedAt]);

  /** 날씨 데이터 가져오기 */
  const fetchWeather = useCallback(async (): Promise<void> => {
    if (!currentLocation) {
      setError('위치 정보가 없어요. 위치를 설정해주세요.');
      return;
    }

    setLoading(true);
    setError(null);

    try {
      // 단기예보 호출
      const items = await fetchShortForecast(
        currentLocation.gridX,
        currentLocation.gridY,
      );

      // 현재 날씨 파싱
      const currentWeather = parseCurrentWeather(items);
      setCurrent(currentWeather);

      // 시간별 예보 파싱
      const hourlyForecasts = parseHourlyForecast(items);
      setHourly(hourlyForecasts);

      // TODO: 주간 예보 (중기예보 API) — v1에서는 단기예보의 3일치 데이터로 대체
      // setDaily(dailyForecasts);

      setLastFetchedAt(Date.now());
    } catch (e) {
      const message = e instanceof Error ? e.message : '날씨 데이터를 가져올 수 없어요.';
      setError(message);
      console.error('Weather fetch error:', e);
    } finally {
      setLoading(false);
    }
  }, [currentLocation, setCurrent, setHourly, setLoading, setError, setLastFetchedAt]);

  /** 앱 포그라운드 복귀 시 조건부 갱신 */
  const refreshIfNeeded = useCallback(async (): Promise<void> => {
    if (needsRefresh()) {
      await fetchWeather();
    }
  }, [needsRefresh, fetchWeather]);

  return {
    current,
    hourly,
    daily,
    isLoading,
    error,
    fetchWeather,
    refreshIfNeeded,
  };
}
```

### 4. Feature Public API

```typescript
// File: src/features/weather-fetch/index.ts
export { useWeatherStore } from './model/useWeatherStore';
export { useWeatherFetch } from './lib/useWeatherFetch';
export type { WeatherState } from './model/types';
```

---

## 완료 기준
- [ ] `src/features/weather-fetch/` 슬라이스 생성 완료
- [ ] `useWeatherStore` — 날씨 데이터 CRUD 동작
- [ ] `useWeatherFetch` — 기상청 API 호출 + 파싱 + 스토어 저장
- [ ] 30분 갱신 간격 로직 동작
- [ ] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] [디바이스] 실제 API 호출 → 날씨 데이터 수신 확인

## 참고
- 이 WO는 WO-003(API 유틸)과 WO-005(Location)에 의존
- features 간 import: `@features/location` → 허용 (FSD에서 같은 레이어 간 import는 원칙적으로 금지이나, store 읽기 전용은 실용적으로 허용)
  - 대안: location을 entities로 내리거나, widget에서 조합
  - 여기서는 실용성을 위해 features 간 읽기 전용 import 허용
- 주간 예보(중기예보)는 v1에서는 단기예보 3일치로 대체
- 참고 문서: `docs/TECHNICAL_DESIGN.md` 섹션 3, 4
