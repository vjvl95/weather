# WO-002: 캐릭터 에셋 배치 및 탭 네비게이션 구조 세팅

## 메타
- **우선순위**: A01
- **상태**: 대기
- **예상 소요**: 1~2시간
- **의존성**: 없음
- **Wave**: 1

---

## 담당 범위 (충돌 방지)

| 작업 | 경로 |
|------|------|
| **생성** | `src/shared/assets/characters/` (이미지 5장) |
| **수정** | `app/(tabs)/_layout.tsx` |
| **생성** | `app/(tabs)/hourly.tsx` |
| **생성** | `app/(tabs)/weekly.tsx` |
| **생성** | `app/city-search.tsx` |

---

## 기술 스택 요약
- **프레임워크**: Expo SDK 55, expo-router
- **스타일링**: NativeWind v4
- **아이콘**: @expo/vector-icons (Ionicons)

---

## 작업 내용

### 1. 캐릭터 이미지 배치

DALL-E로 생성한 몽글이 이미지 5장을 배치합니다.
배경 제거 후 PNG로 저장해야 합니다.

```
src/shared/assets/characters/
├── monggeul-sunny.png
├── monggeul-cloudy.png
├── monggeul-rainy.png
├── monggeul-snowy.png
└── monggeul-night.png
```

> 이미지 권장 사이즈: 512x512px, 배경 투명 PNG

### 2. 하단 탭 레이아웃 변경 (4탭)

```typescript
// File: app/(tabs)/_layout.tsx (수정)
import { Tabs } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';

export default function TabLayout() {
  return (
    <Tabs
      screenOptions={{
        headerShown: false,
        tabBarActiveTintColor: '#4A90D9',
        tabBarInactiveTintColor: '#8E8E93',
        tabBarStyle: {
          backgroundColor: '#FFFFFF',
          borderTopColor: '#E0E0E0',
        },
      }}
    >
      <Tabs.Screen
        name="index"
        options={{
          title: '홈',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="home-outline" size={size} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="hourly"
        options={{
          title: '시간별',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="time-outline" size={size} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="weekly"
        options={{
          title: '주간',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="calendar-outline" size={size} color={color} />
          ),
        }}
      />
      <Tabs.Screen
        name="settings"
        options={{
          title: '설정',
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="settings-outline" size={size} color={color} />
          ),
        }}
      />
    </Tabs>
  );
}
```

### 3. 시간별 예보 라우트 (thin wrapper)

```typescript
// File: app/(tabs)/hourly.tsx
import { HourlyPage } from '@pages/hourly';

export default function HourlyRoute() {
  return <HourlyPage />;
}
```

### 4. 주간 예보 라우트 (thin wrapper)

```typescript
// File: app/(tabs)/weekly.tsx
import { WeeklyPage } from '@pages/weekly';

export default function WeeklyRoute() {
  return <WeeklyPage />;
}
```

### 5. 도시 검색 모달 라우트

```typescript
// File: app/city-search.tsx
import { CitySearchPage } from '@pages/city-search';

export default function CitySearchRoute() {
  return <CitySearchPage />;
}
```

### 6. RootLayout에 city-search 모달 추가

```typescript
// File: app/_layout.tsx (수정 — Stack.Screen 추가)
// 기존 Stack 안에 추가:
<Stack.Screen name="city-search" options={{ presentation: 'modal' }} />
```

---

## 완료 기준
- [ ] 캐릭터 이미지 5장 배치 완료 (`src/shared/assets/characters/`)
- [ ] 하단 탭바 4개 탭 (홈/시간별/주간/설정) 동작
- [ ] `app/(tabs)/hourly.tsx` 생성 완료
- [ ] `app/(tabs)/weekly.tsx` 생성 완료
- [ ] `app/city-search.tsx` 모달 라우트 생성 완료
- [ ] TypeScript 컴파일 에러 없음 (`npx tsc --noEmit`)
- [ ] [디바이스] 하단 탭 전환 동작 확인
- [ ] [디바이스] 도시 검색 모달 열림/닫힘 확인

## 참고
- 이미지는 사용자가 DALL-E에서 생성 → 배경 제거 → 배치
- 라우트 파일은 thin wrapper만 — UI 로직은 pages 레이어에서 구현 (WO-007, 008, 009)
- detail 라우트는 이 앱에서 불필요 → 삭제 가능 (선택)
- 참고 문서: `docs/PRD.md` 섹션 4, `docs/TECHNICAL_DESIGN.md` 섹션 6
